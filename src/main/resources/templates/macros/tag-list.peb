{# @pebvariable name="tags" type="java.util.List<ch.fhnw.webec.frageundantwortapp.model.Tag>" #}

{% macro tags(tags) %}
        <ul>
                {% for tag in tags %}
                <li>
                        {{ tag.name }}
                </li>
                {% endfor %}
        </ul>
{% endmacro %}
